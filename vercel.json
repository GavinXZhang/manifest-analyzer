{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/app" }],
  "functions": {
    "api/app.mjs": {
      "includeFiles": "{web/dist/**,dist/**}",
      "maxDuration": 30
    }
  }
}
