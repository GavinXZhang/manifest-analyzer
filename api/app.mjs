// Vercel function entry. The real server is bundled by `npm run bundle:vercel`
// into dist/server.mjs during the build; this stub exists in git so Vercel can
// see the function before the build runs.
export { default } from '../dist/server.mjs';
